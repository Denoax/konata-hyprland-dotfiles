#!/usr/bin/env bash

selection=$(cliphist list | rofi -dmenu -i -p 'CLIPBOARD' -theme ~/.config/rofi/konata.rasi) || exit 0
[[ -n "$selection" ]] || exit 0
printf '%s' "$selection" | cliphist decode | wl-copy
